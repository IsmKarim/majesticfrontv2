# Base URL of the property API. Absolute in every deployed environment;
# the relative "/api" fallback in siteConfig only works for same-origin routes.
NEXT_PUBLIC_API_URL=https://api.majestickeys.com/v1
